@model IEnumerable<proyecto_inventario.Models.Inventario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InvEntrada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InvSalida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InvStock)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InvEntrada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InvSalida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InvStock)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
